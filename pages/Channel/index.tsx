import React from 'react';
import Workspace from '@layouts/Workspace';
import { Container, Header } from '@pages/Channel/styles';

const Channel = () => {
  return (
    <Workspace>
      <Container>
        <Header>로그인 한 것을 축하 드려요</Header>
      </Container>
    </Workspace>
  );
};

export default Channel;
